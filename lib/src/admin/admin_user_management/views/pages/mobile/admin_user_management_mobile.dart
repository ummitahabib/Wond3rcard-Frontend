import 'package:flutter/material.dart';
import 'package:hooks_riverpod/hooks_riverpod.dart';

class AdminUserManagementMobile extends HookConsumerWidget {
  const AdminUserManagementMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Placeholder();
  }
}
